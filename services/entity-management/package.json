{
  "name": "gep-entity-management",
  "version": "1.0.0",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "build": "mkdir -p build && zip -r build/handler.zip index.mjs handlers/ utils/ package.json"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145"
  }
}